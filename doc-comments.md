# Padrão de Doc-Comments Automáticos

Este documento define o padrão recomendado para documentação automática de código gerado pela ferramenta **leds-tools-spark**.

---

##  Visão Geral

Todas as classes e métodos gerados automaticamente devem incluir comentários de documentação que identifiquem:
- A origem da geração (`@generated by leds-tools-spark`)
- A camada arquitetural (`@layer`)
- A entidade relacionada (`@entity`)
- Operações e parâmetros quando aplicável

---

##  1. Controller

Essas classes são os pontos de entrada do CRUD, portanto é importante registrar o tipo de operação e a entidade.

### Exemplo:

```java
/**
 * @generated by leds-tools-spark
 * @layer controller
 * @entity User
 */
public class UserController {
    
    /**
     * @generated by leds-tools-spark
     * @operation create
     * @entity User
     * @param name String
     * @param email String
     * @param password String
     * @return User
     */
    public User createUser(String name, String email, String password) {
        // lógica
    }
}
```


##  2. Service

A estrutura é quase igual ao Controller, mas com `@layer service`.

### Exemplo:

```java
/**
 * @generated by leds-tools-spark
 * @layer service
 * @entity User
 */
public class UserService {
    
    /**
     * @generated by leds-tools-spark
     * @operation create
     * @entity User
     * @param user User
     * @return User
     */
    public User createUser(User user) {
        // lógica
    }
}
```


##  3. Repository

Esses normalmente só estendem interfaces, então basta marcar a origem e o tipo de camada.

### Exemplo:

```java
/**
 * @generated by leds-tools-spark
 * @layer repository
 * @entity User
 */
public interface UserRepository extends JpaRepository<User, Long> { }
```


## 🟧 4. Model / Entity

Se não houver uso futuro de análise automática, mantenha o mais simples possível.

### Exemplo:

```java
/**
 * @generated by leds-tools-spark
 * @layer model
 * @entity User
 */
public class User {
    private Long id;
    private String name;
    private String email;
    private String password;
}
```


## 🧠 Vantagens desse Padrão

✅ Curto e legível  
✅ Fácil de gerar automaticamente  
✅ Consistente entre todas as camadas  
✅ Permite futura análise automatizada (por regex, parser ou IA)  
✅ Pode ser expandido no futuro (ex: adicionar `@version`, `@author`, etc.)

---

## 🧾 Resumo do Padrão

| Camada     | Tags recomendadas                                                      | Nível de detalhe |
|------------|------------------------------------------------------------------------|------------------|
| Controller | `@generated`, `@layer controller`, `@entity`, `@operation`, `@param`, `@return` | Completo         |
| Service    | `@generated`, `@layer service`, `@entity`, `@operation`, `@param`, `@return`    | Completo         |
| Repository | `@generated`, `@layer repository`, `@entity`                           | Básico           |
| Model      | `@generated`, `@layer model`, `@entity`                                | Básico           |

